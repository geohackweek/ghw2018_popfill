class Basic(object):

    def fill(this, mask, counts):
        return counts.copy()

    def pop_to_mask(pop):
        '''this function generates a boolean mask based on the population map'''
        return pop == pop

